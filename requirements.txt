# RAMC — Risk-Aware Model-then-Control
# Python >= 3.9 recommended (uses pathlib, typing, dataclasses, __future__ annotations)
#
# These are minimum-tested constraints. For an exact reproduction record,
# replace this file with the output of `pip freeze` from your working
# environment before tagging the release.

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
torch>=2.0
scikit-learn>=1.3
tqdm>=4.65

# External tool (not a pip package), required ONLY for Stage 1
# (EnergyPlus data generation): EnergyPlus v25.1
#   https://energyplus.net/
# Stages 2–8 run entirely on the identified RC model and do not need EnergyPlus.
